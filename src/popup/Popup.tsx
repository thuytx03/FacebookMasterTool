import { MemoryRouter } from 'react-router-dom'
import { Routing } from './routers'

export const Popup = () => {

  return (
    <MemoryRouter>
      <Routing />
    </MemoryRouter>
  )
}

export default Popup
